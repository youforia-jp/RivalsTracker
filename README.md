# Marvel Rivals — Esports Draft Analytics Pipeline

> **Automatically pull tracker.gg stats for every player in your draft pool and export them to a clean CSV — ready for captain review, seeding, or team-building.**

---

## 📋 Table of Contents

- [Overview](#overview)
- [How It Works](#how-it-works)
- [Project Structure](#project-structure)
- [Prerequisites](#prerequisites)
- [Setup & Installation](#setup--installation)
- [Input Format](#input-format)
- [Running the Pipeline](#running-the-pipeline)
- [Output Format](#output-format)
- [Sample Data](#sample-data)
- [Rate Limiting & Cloudflare](#rate-limiting--cloudflare)
- [Disclaimer](#disclaimer)

---

## Overview

When running an esports draft for a Marvel Rivals community tournament, manually looking up every player's stats on [tracker.gg](https://tracker.gg/marvel-rivals) is painfully slow. This pipeline automates the entire process:

1. Add player IGNs to `IGNs.csv`
2. Run `Tracker.py`
3. Open `IGN_stats.csv` — every player's rank, KDA, win rate, top heroes, and role breakdown, ready for your captains

No API key required. Works by bypassing Cloudflare protections via [`cloudscraper`](https://github.com/VeNoMouS/cloudscraper).

---

## How It Works

```
IGNs.csv                  Tracker.py               IGN_stats.csv
┌─────────────────┐       ┌──────────────────┐      ┌──────────────────────┐
│  IGN            │──────▶│ cloudscraper.get  │─────▶│ PLAYER OVERVIEW      │
│  IGN            │       │ tracker.gg API   │      │ HERO BREAKDOWN       │
│  IGN            │       │                  │      │ ROLE WR SUMMARY      │
│  ...            │       │ parse segments[] │      │                      │
└─────────────────┘       │ top 5 heroes     │      │ (3-section CSV)      │
                          └──────────────────┘      └──────────────────────┘
```

### API Response Structure

The tracker.gg API returns a list of **segments**:

| Segment | Type | Contents |
|--------|------|----------|
| `segments[0]` | `overview` | KDA, Win Rate, Matches Played, Current Rank, Season Peak Rank |
| `segments[1..N]` | `hero` | Per-hero stats — sorted by matches played (most first) |

The pipeline grabs the overview stats + the top 5 heroes by matches played.

---

## Project Structure

```
Esports-Draft-Analytics-Pipeline/
│
├── Tracker.py                  # Main pipeline script
├── debug_api.py                # One-shot API tester for a single IGN
│
├── IGNs.csv                    # 📝 Public template — committed to GitHub
├── Master_IGNs.csv             # ⚠️ YOUR PRIVATE INPUT (gitignored, script checks this first)
├── IGN_stats.csv               # ⚠️ YOUR OUTPUT — generated by the script (gitignored)
│
├── samples/
│   ├── ign_SAMPLE.csv          # Example input showing the format
│   └── IGN_stats_SAMPLE.csv    # Example output showing the 3-section format
│
├── requirements.txt            # Python dependencies
├── .gitignore                  # Excludes Master_IGNs.csv and IGN_stats.csv from git
├── LICENSE                     # MIT License
└── README.md                   # You are here
```

> ⚠️ **`Master_IGNs.csv` and `IGN_stats.csv` are gitignored** — your real player data never leaves your machine.

---

## Prerequisites

- Python 3.8+
- pip

---

## Setup & Installation

```bash
# 1. Clone the repo
git clone https://github.com/YOUR_USERNAME/marvel-rivals-draft-pipeline.git
cd marvel-rivals-draft-pipeline

# 2. (Optional but recommended) Create a virtual environment
python -m venv venv
venv\Scripts\activate        # Windows
# source venv/bin/activate   # macOS / Linux

# 3. Install dependencies
pip install -r requirements.txt
```

---

## Input Format

The script prioritizes a file called `Master_IGNs.csv` (which is gitignored so it stays private on your machine). If that isn't found, it falls back to the public `IGNs.csv` template.

Open or create `Master_IGNs.csv`. Add one IGN per line below the header:

| Column | Field | Description |
|--------|-------|-------------|
| A | `IGN` | Player's in-game name — used for the tracker.gg API lookup |

**`Master_IGNs.csv` format:**
```
IGN
Flats
ml7support
YourPlayerHere
...
```

The script skips the header row and any blank rows. No roles, ranks, or Discord handles needed — just the IGN.

See [`samples/ign_SAMPLE.csv`](samples/ign_SAMPLE.csv) for a reference example with 20 known players.

---

## Running the Pipeline

```bash
# Reads Master_IGNs.csv (or IGNs.csv), writes IGN_stats.csv
python Tracker.py
```

Progress is printed to the console as each player is fetched:

```
Fetching stats for Flats...
  Waiting 13.4s before next request...
Fetching stats for ml7support...
  Waiting 15.1s before next request...
...
Done! Check IGN_stats.csv
```

> ⏱ With the built-in rate limiting, expect roughly **12–17 seconds per player**. A 20-player pool takes ~5 minutes.

---

## Output Format

`IGN_stats.csv` has **three sections**:

**Section 1 — Player Overview** (one row per player)

| Column | Description |
|--------|-------------|
| `IGN` | Player's in-game name |
| `Main_Role` | Role with the most matches played (Vanguard / Duelist / Strategist) |
| `Current_Rank` | Player's current ranked tier (e.g. `Grandmaster III`) |
| `Season_Peak_Rank` | Highest rank reached this season (e.g. `Celestial I`) |
| `Overall_KDA` | Overall KDA across all heroes |
| `Overall_WinRate%` | Overall win rate |
| `Total_Matches` | Total matches on record |
| `Status` | `Success`, `Private/Not Found`, or `Error: <code>` |

**Section 2 — Hero Breakdown** (one row per player × top-5 hero, ranked by matches)

| Column | Description |
|--------|-------------|
| `Hero_Rank` | 1 = most-played hero |
| `Hero` | Hero name |
| `Role` | Hero's class (Vanguard / Duelist / Strategist / Multi-Role) |
| `KDA` | KDA on this hero |
| `WinRate%` | Win rate on this hero |
| `Matches` | Matches played on this hero |

**Section 3 — Role Win Rate Summary** (one row per player × role)

| Column | Description |
|--------|-------------|
| `Role` | Vanguard, Duelist, or Strategist |
| `Weighted_WinRate%` | Match-weighted average WR across all heroes in that role |
| `Role_Matches` | Total matches played in that role |

> Multi-Role heroes (e.g. Deadpool) appear in the hero breakdown but are excluded from the role WR summary since they don't belong to a single class.

See [`IGN_stats_SAMPLE.csv`](IGN_stats_SAMPLE.csv) for example output.

---

## Sample Data

| File | Description |
|------|-------------|
| [`samples/ign_SAMPLE.csv`](samples/ign_SAMPLE.csv) | Example input — 20 known Marvel Rivals players/streamers with public profiles |
| [`samples/IGN_stats_SAMPLE.csv`](samples/IGN_stats_SAMPLE.csv) | Example output showing the full 3-section format |

> Sample output contains fabricated numbers for demo purposes. Fill in `IGNs.csv` and run the pipeline to get live data.

---

## Rate Limiting & Cloudflare

- **12–17 second delay** (with random jitter) between requests to avoid rate limiting
- Retries up to 3× on 429 with exponential backoff (60s / 120s / 180s)
- Creates a **fresh session per player** so a blocked session doesn't cascade
- Uses [`cloudscraper`](https://github.com/VeNoMouS/cloudscraper) with Chrome browser headers to handle Cloudflare
- Validates the response body is real JSON before parsing — gracefully handles Cloudflare challenge pages
- If a player's profile is private or not found, writes `Private/Not Found` rather than crashing

---

## Disclaimer

This project is a **personal/community tool** built for fun and esports draft organization. It is:

- **Not affiliated with** Netease, Marvel, or tracker.gg
- **Not intended for commercial use**
- Dependent on tracker.gg's public API — if they change their API or add stricter rate limits, the script may break
- Respectful of rate limits — please do not abuse it

Use responsibly. 🦸
